# Backend (Railway service)
PORT=8501
OPENAI_API_KEY=

# Streamlit runtime
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_PORT=8501

# CORS (if API service is added separately later)
BACKEND_CORS_ORIGINS=https://your-service.up.railway.app,http://localhost:3000
